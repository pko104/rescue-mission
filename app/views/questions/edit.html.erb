<br>
  <%= form_for @question, method: :patch do |f| %>
    <div>
      <%= f.label "EDIT YO QUEESTION", as: :name %>
      <br>
        <%= f.text_area :name, size: "40x5" %>
    </div>
    <div>
          <%= f. label :description %>
      <br>
        <%= f. text_area :description, size: "40x10" %>
    </div>
    <div>
      <%= f.submit  "Submit"%>
    <div>
  <%end%>
</br>

<%=link_to "Go Back", question_path(id: @question.id)%>
